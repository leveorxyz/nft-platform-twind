const Logo = () => {
  return (
    <div className="text-3xl font-bold bg-clip-text uppercase text-bold logo-gradient tracking-wider">
      NFT.MARKET
    </div>
  );
};

Logo.defaultProps = {
  fontSize: '32px',
};

export default Logo;
